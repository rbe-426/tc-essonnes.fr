{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "cd server && npm install && npm run build",
    "startCommand": "cd server && PORT=8081 node dist/index.js"
  }
}
